# Custom Nav Control (NetBox Plugin)

This NetBox plugin hides the IPAM menu for non-superusers.

## Installation

1. Copy this folder into your NetBox plugins directory or build context.
2. Install the plugin inside the NetBox container:
   ```bash
   pip install /plugins/custom_nav_control
